function assertEqual(actual, expected, testName) {
   if (actual === expected) {
     console.log('passed');
   } else {
     console.log('FAILED ' + testName + ': Expected ' + expected + ', but got ' + actual);
   }
}
object = {a:1,b:2,c:3,D:4,e:5,f:600}

function findAvg(object) {
    for (var i = 0; i < object.length; i++) {
      total = valueOf(i){
        object.Math.sum(key)
      }

}
